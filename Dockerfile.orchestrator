FROM python:3.12-slim

WORKDIR /workspace

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

CMD ["python3", "-c", "import time; print('Orchestrator container running...'); time.sleep(86400)"]
