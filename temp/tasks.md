# Tasks

1. Scaffold Cargo project in `tools/swarms-orchestrator`.
2. Write `Cargo.toml` with dependencies.
3. Write `src/main.rs` with ConcurrentWorkflow logic.
4. Run `cargo check` and `cargo fmt`.
