apiVersion: 1

providers:
  - name: "Arch-Systems Dashboards"
    orgId: 1
    folder: "Arch-Systems"
    folderUid: "arch-systems"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
