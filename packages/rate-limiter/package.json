{
  "name": "@repo/rate-limiter",
  "description": "Rate limiting framework with multiple strategies (fixed-window, sliding-window, token-bucket) and stores (memory, Redis) using dependency injection",
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "@repo/typescript-config": "1.0.0",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30.0.0",
    "@types/node": "catalog:",
    "jest": "^30.0.0",
    "typescript": "catalog:"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist .nx",
    "dev": "tsc --watch",
    "lint": "eslint . --cache --cache-location node_modules/.cache/.eslintcache --max-warnings 0",
    "type-check": "tsc --noEmit",
    "test": "jest --passWithNoTests"
  },
  "types": "./src/index.ts"
}
