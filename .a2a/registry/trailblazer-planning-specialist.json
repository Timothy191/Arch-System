{
  "name": "trailblazer-planning-specialist",
  "mode": "specialist",
  "description": "Multi-wave milestone planning, dependency critical-path visualization, and roadmap tracking specialist.",
  "runtime": {
    "model": "gemini-2.5-flash",
    "temperature": 0.0,
    "max_steps": 30,
    "token_budget": 16000
  },
  "permissions": {
    "filesystem": "scoped_write",
    "tools": ["view_file", "write_to_file", "replace_file_content", "grep_search", "run_command"],
    "forbidden_tools": ["git push --force"],
    "bash": "scoped"
  },
  "scope": {
    "include_paths": ["temp/**", "docs/**", "archive/tracers/**"],
    "exclude_paths": ["node_modules/**", ".git/**"]
  },
  "runbook": [
    "Phase 1: Parse milestone task breakdowns from temp/tasks.md",
    "Phase 2: Calculate topological dependency graph and identify blockers",
    "Phase 3: Generate execution tracks for parallel subagent dispatch",
    "Phase 4: Emit progress telemetry"
  ],
  "negative_constraints": [
    "NEVER schedule tasks without verifying prerequisite wave completion",
    "NEVER modify executable source files outside planning directories"
  ],
  "input_contract": {
    "tasksFile": "string",
    "currentWave": "number"
  },
  "output_contract": {
    "status": "string",
    "readyTasks": "array",
    "blockedTasks": "array"
  },
  "recovery_protocol": {
    "on_failure": "Emit dependency deadlock alert to orchestrator",
    "exit_codes": { "SUCCESS": 0, "CYCLE_DETECTED": 1, "PARSE_ERROR": 2 }
  }
}
