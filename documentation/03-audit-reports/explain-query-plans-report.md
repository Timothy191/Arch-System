# PostgreSQL Query Plan & Partition Foreign Key Audit

Generated by `tools/explain-query-plans.cjs` from `packages/database/migrations`.

## Partitioned Tables Summary

| Table             | Partition Key    | Primary Key            | PK Partition Aligned | Migration                           |
| ----------------- | ---------------- | ---------------------- | -------------------- | ----------------------------------- |
| `hourly_loads`    | `load_date`      | `(id, load_date)`      | ✅ Yes               | `020_partition_time_series.sql`     |
| `daily_logs`      | `log_date`       | `(id, log_date)`       | ✅ Yes               | `020_partition_time_series.sql`     |
| `production_logs` | `daily_log_date` | `(id, daily_log_date)` | ✅ Yes               | `072_partition_production_logs.sql` |

## Composite Foreign Key Constraints (Partition Aligned)

| Constraint                   | Source Table FK                  | Target Table | Target Primary Key | Composite Aligned | Migration                            |
| ---------------------------- | -------------------------------- | ------------ | ------------------ | ----------------- | ------------------------------------ |
| `badges_fleet_id_fkey`       | `(fleet_id)`                     | `fleet`      | `(id)`             | ℹ️ Single         | `035_fleet_and_equipment_tables.sql` |
| `badges_equipment_id_fkey`   | `(equipment_id)`                 | `equipment`  | `(id)`             | ℹ️ Single         | `035_fleet_and_equipment_tables.sql` |
| `fk_production_daily_log`    | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_machine_hours_daily_log` | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_fuel_logs_daily_log`     | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_excavator_daily_log`     | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_dozer_rolls_daily_log`   | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_hourly_loads_daily_log`  | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_machine_ops_daily_log`   | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `071_operational_immutability.sql`   |
| `fk_production_daily_log`    | `(daily_log_id, daily_log_date)` | `daily_logs` | `(id, log_date)`   | ✅ Composite      | `072_partition_production_logs.sql`  |

## Performance & EXPLAIN ANALYZE Guidelines

- **Partition Pruning**: Queries filtering by `load_date` or `daily_log_date` filter out non-matching monthly partitions prior to execution scan.
- **Foreign Key Integrity**: Enforcing composite `(parent_id, partition_date)` foreign keys ensures referential integrity across partitioned PostgreSQL child tables.
- **Index Scalability**: Each monthly partition retains independent B-Tree index structures, avoiding bloat and maintaining $O(\log N)$ lookup efficiency.
